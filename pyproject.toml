[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "snowlink"
version = "0.1.0"
description = "Private LAN screen and system-audio share for Windows 11"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "Proprietary" }
authors = [{ name = "Snowlink Contributors" }]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
    "mypy>=1.11",
    "pyinstaller>=6.0",
]
capture = [
    "dxcam[cv2,winrt]>=0.3.0",
    "opencv-python>=4.8",
    "numpy>=1.26",
    "psutil>=5.9",
]
audio = [
    "PyAudioWPatch>=0.2.12.6",
    "av>=12.0",
    "numpy>=1.26",
    "psutil>=5.9",
]
webrtc = [
    "aiortc>=1.9.0",
    "aiohttp>=3.9.0",
    "av>=12.0",
    "opencv-python>=4.8",
    "numpy>=1.26",
    "psutil>=5.9",
]
ui = [
    "PySide6>=6.6",
]

[project.scripts]
snowlink = "snowlink.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "integration: local network/socket integration tests",
    "hardware: optional tests requiring real DXcam / desktop capture or WASAPI audio",
    "network: optional tests requiring real LAN / two-machine networking",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["snowlink"]

[[tool.mypy.overrides]]
module = ["dxcam", "dxcam.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["PySide6", "PySide6.*"]
ignore_missing_imports = true

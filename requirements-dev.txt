# Authoritative dependency versions live in pyproject.toml.
# Do not add or pin package versions here.
#
# Maintain this file as a thin installer shim only:
#   - Dev tools:       [project.optional-dependencies].dev in pyproject.toml
#   - Install command: pip install -e ".[dev]"
#
# Includes the runtime package (editable) plus pytest, pytest-asyncio, ruff, and mypy.
# For Experiment C screen capture, also install: pip install -e ".[capture]"
# For Experiment D WASAPI loopback audio, also install: pip install -e ".[audio]"
# For Experiment E synthetic WebRTC video, also install: pip install -e ".[webrtc]"
# For Experiment F synthetic WebRTC Opus audio, also install: pip install -e ".[webrtc]"
#   (add .[audio] when audible WASAPI playback is needed)
# For the PySide6 app shell: pip install -e ".[ui]"
# Full local GUI + capture + experiments:
#   pip install -e ".[dev,ui,capture,audio,webrtc]"

-e ".[dev]"
